export enum ERole {
  admin = 'admin',
  manager = 'manager',
  member = 'member',
}

export enum EGender {
  male = 'male',
  female = 'female',
}

export enum EUserStatus {
  banned = 'banned',
  activated = 'activated',
}
